name: CI

# This is a minimal placeholder pipeline for Lab 0.
# You will extend this workflow starting in Lab 3 (baseline CI/CD pipeline)
# and keep adding security controls to it in every lab through Lab 12.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Confirm pipeline runs
        run: echo "CI pipeline stub is working. Extend this in Lab 3."
